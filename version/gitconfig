# vim: set filetype=conf :

# Commentaires : # ou ; en début de ligne

# {{{ Core

[core]

pager = less --quit-if-one-screen
#pager = "diff-so-fancy | less --tabs=4 -RFX"

editor = vim

attributesfile = ~/racine/config/version/gitattributes

excludesfile = ~/racine/common/target/gitignore

autocrlf = input

whitespace = blank-at-eol,blank-at-eof

# }}}

# {{{ Help

autocorrect = 1

# }}}

# {{{ User

[user]

name = user_name

email = user_name@mail_server.com

#signingkey = 9307AC23

# }}}

# {{{ Pager

[pager]

status = false

log = true
diff = true

show-branch = false

reflog = false

# }}}

# {{{ Commit

[commit]

template=~/racine/config/version/gitmessage

# }}}

# {{{ Diff

[diff]

tool = vimdiff

[diff "image"]

command = exiv2 pr

[diff "word"]

command = antiword

[diff "zip"]

textconv = unzip -c -a

# }}}

# {{{ Merge

[merge]

tool=vimdiff
conflictstyle=diff3

[mergetool]

prompt=false

# }}}

# {{{ Rerere

[rerere]

enabled=true

# }}}

# {{{ Repack

[repack]

usedeltabaseoffset = true

# }}}

# {{{ Credential

[credential]

helper = cache --timeout=3600

# }}}

# {{{ Format

[format]

numbered = auto

# }}}

# {{{ Color

[color]

ui = auto
interactive = auto

# }}}

# {{{ Alias

[alias]

i    = commit
o    = checkout
ci   = commit
co   = checkout
li   = commit
lo   = checkout
lci  = commit
lco  = checkout
ri   = push
ro   = pull
rci  = push
rco  = pull

a    = add
b    = branch
c    = clone
d    = diff
dt   = difftool
dc   = diff --cached
ds   = diff --stat
f    = fetch
h    = help
ii   = init
l    = log --stat --decorate --abbrev-commit --relative-date --all --graph
ll   = log --oneline
ls   = ls-files
m    = merge
mt   = mergetool
mf   = merge-file
p    = pull
r    = remote
s    = status
#s    = status -s
t    = tag
sw   = show
z    = gc

last = log -1 HEAD

stage = add
unstage = reset HEAD --

apply-patch       = apply
apply-mail        = am

tree = ! tree . | less

# }}}

# Github {{{1

[github]

user = user_name

# }}}1
