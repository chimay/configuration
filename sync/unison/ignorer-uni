# vim: set filetype=conf :

# {{{ Self

ignorenot = Regex racine/config/sync/unison/archive/[^/]+/ar[0-9a-f]*
ignorenot = Regex racine/config/sync/unison/archive/[^/]+/fp[0-9a-f]*

ignorenot = Path racine/syncron/cleusb

# Étiquettes {{{2

ignorenot = Name tags
ignorenot = Name TAGS

ignorenot = Name GTAGS
ignorenot = Name GRTAGS
ignorenot = Name GPATH
ignorenot = Name ID
ignorenot = Name gtags.files

# {{{ Bases de données

ignorenot = Path racine/index/locate/*
ignorenot = Path racine/index/locate/.*

#ignorenot = Path racine/index/filesys/z-dirs

# {{{ Contrôle de version

# À décommenter de temps en temps

#ignorenot = Name .git

#ignorenot = Regex racine/plugin/manager/.*/.git
#ignorenot = Regex racine/plugin/manager/.*

# {{{ Courriel

ignorenot = Path racine/mail/Procmail/Sauvegarde

ignorenot = Path racine/mail/Maildir

# Traffic mail local à la machine

ignorenot = Path racine/mail/Systeme/mbox
ignorenot = Path racine/mail/Systeme/dead.letter

# {{{ Extensions

ignorenot = Path racine/plugin/manager/plugged/*
ignorenot = Path racine/plugin/manager/plugged/.*

ignorenot = Path racine/plugin/manager/vim-plugged/*
ignorenot = Path racine/plugin/manager/vim-plugged/.*

ignorenot = Path racine/plugin/manager/el-get/*
ignorenot = Path racine/plugin/manager/el-get/.*

ignorenot = Path racine/plugin/manager/elpaca/*
ignorenot = Path racine/plugin/manager/elpaca/.*

ignorenot = Path racine/plugin/manager/straight/*
ignorenot = Path racine/plugin/manager/straight/.*

ignorenot = Path racine/plugin/source/*
ignorenot = Path racine/plugin/source/.*

# AUR {{{1

ignore = Path racine/pack/aur/*
ignore = Path racine/pack/aur/.*

# Provisoire {{{1

ignorenot = Path racine/config/windenv/xfce4/*
ignorenot = Path racine/config/windenv/xfce4/.*
